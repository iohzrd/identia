[package]
name = "app"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
license = ""
repository = ""
default-run = "app"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
tauri-build = { version = "1.0.0-beta.4" }

[dependencies]
chrono = "0.4"
directories = "3.0.2"
futures = "0.3.16"
ipfs-api = "0.11.0"
percent-encoding = "2.1.0"
rusqlite = { version = "0.25.1", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.66"
tauri = { version = "1.0.0-beta.6", features = ["api-all", "cli", "system-tray", "updater"] }
tokio = { version = "1.9", features = [ "full", "rt", "rt-multi-thread", "sync", "fs" ] }

[features]
default = [ "custom-protocol" ]
custom-protocol = [ "tauri/custom-protocol" ]
